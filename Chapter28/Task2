IF A <> 0 
    THEN
        B <- A
    ELSE
        B <- B-1

Assembly Code:

        LDD A
        CMP #0
        JPE ELSE
        STO B
        JMP ENDIF
        DEC ACC
        STO B
        JMP ENDIF
ELSE:   DEC ACC

ENDIF:  ...


